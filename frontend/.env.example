# Free Mapbox account -> https://account.mapbox.com/access-tokens/
# Restrict this token to your dev/prod URLs in the Mapbox dashboard once deployed.
VITE_MAPBOX_TOKEN=

# Base URL of the backend API (see backend/.env.example for the matching PORT)
VITE_API_BASE_URL=http://localhost:8787
